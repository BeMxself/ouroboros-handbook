---
sidebar_position: 2
---

# AMIS 开发参考

这页适合已经确认要用 AMIS 做页面、组件或交互配置，但还在判断“该看官方文档，还是该看潮汐栈自己的前端扩展文档”的开发者。

在潮汐栈里，AMIS 不是孤立使用的。它通常会和下面几类能力一起出现：

- 低代码页面与 UI Schema
- 平台基座内建组件
- 微前端 SDK
- 组件库扩展

所以这页更像“导航页 + 小贴士页”，而不是完整替代官方 AMIS 文档。

## 先判断你现在遇到的是哪类问题

| 当前问题                                       | 优先去看哪里                                                     |
| ---------------------------------------------- | ---------------------------------------------------------------- |
| 我不知道某个 Schema 怎么写                     | 先看 [AMIS 官方文档](https://baidu.github.io/amis/zh-CN/docs/index) |
| 我想知道潮汐栈基座已经内建了哪些前端组件       | 看 [前端 API](../../../api/frontend/) 和 [基座内建组件](../../../api/frontend/buildin-comps/) |
| 我想在微前端里调用平台对话框、抽屉、事件或工具 | 看 [前端 API](../../../api/frontend/) 和 [SDK 文档](../../../api/frontend/sdk/) |
| 我需要在低代码页面里注册新的可复用控件         | 看 [组件库](../extend-frontend/amis-component/)                 |
| 我还没想清楚该写 UI Schema 还是做前端扩展      | 看 [前端融合开发方式](../../../fusion-development/frontend-extension-paths) 和 [前端 FAQ](../../../faq/frontend/) |

## 在潮汐栈里，AMIS 通常承担什么角色

你可以把 AMIS 理解成“平台前端里的结构化界面描述层”。

它最适合承接：

- 列表、表单、详情、操作面板这类结构化页面
- 需要通过 Schema 快速交付和迭代的业务界面
- 基于平台现成组件和运行时能力拼装出来的页面

它不太适合直接承接：

- 大量自定义状态管理和复杂交互的整页应用
- 独立路由、复杂图形交互或强工程化的前端产品
- 本质上更适合微前端页面的场景

如果你发现 Schema 越写越像“在 JSON 里硬写一整套前端应用逻辑”，通常就该回头评估是否应该切到微前端路线。

## 什么时候优先看官方文档

下面这些问题，大概率应该先回到官方 AMIS 文档：

- 某个组件原生支持哪些属性
- 事件动作、表达式、数据映射的基础语法
- 表单校验、联动、可见性控制的通用写法
- 组合容器、布局和渲染器的标准能力

这是因为这些问题和“潮汐栈”无关，而是 AMIS 自身能力边界。

## 什么时候优先看潮汐栈自己的文档

下面这些问题，更应该看潮汐栈自己的手册或 API 页：

- 平台基座已经注入了哪些组件、动作和 SDK 能力
- 微前端应用里如何渲染 AMIS Schema
- 低代码页面和自定义前端扩展怎么组合
- 平台里怎样注册 AMIS 自定义组件、动作、公式或校验器

推荐顺序通常是：

1. 先用官方文档确认“AMIS 本身能不能做”
2. 再用潮汐栈文档确认“平台里应该怎么接”

## 一条很实用的判断线

### 只是写 Schema

优先看官方 AMIS 文档，再结合平台内建组件和现成页面模式。

### 需要调用平台能力

优先看 [前端 API](../../../api/frontend/)，尤其是 SDK 和基座组件相关内容。

### 需要做自定义控件

优先看 [扩展前端](../extend-frontend/) 里的 [组件库](../extend-frontend/amis-component/)。

### 需要做整页复杂交互

先回到 [前端融合开发方式](../../../fusion-development/frontend-extension-paths)，判断是否应该用微前端页面，而不是继续堆 Schema。

## 开发小贴士

### 事件动作里的 expression 上下文

在 AMIS 事件动作里写 `expression` 时，常见可用上下文可以这样理解：

1. 事件自身的 `data`
   例如通过 `broadcast` 动作广播事件时，可以在 `data` 中传值；监听该事件的动作表达式里可以直接读取这些字段。
2. 当前动作所在位置的数据域
   例如这个动作挂在表单里，就通常可以直接访问表单数据。
3. `event` 对象
   它包含事件本身的类型、数据以及上下文信息。

如果事件的 `data` 和当前数据域里出现同名字段，通常需要显式通过 `__super.xxx` 或 `__rendererData.xxx` 来区分来源，避免表达式读错值。

## 推荐阅读顺序

1. [AMIS 官方文档](https://baidu.github.io/amis/zh-CN/docs/index)
2. [前端 API](../../../api/frontend/)
3. [SDK 文档](../../../api/frontend/sdk/)
4. [扩展前端](../extend-frontend/)
5. [组件库](../extend-frontend/amis-component/)

## 下一步看哪里

- 想先判断该不该写前端扩展：看 [前端融合开发方式](../../../fusion-development/frontend-extension-paths)
- 想直接查平台前端能力：看 [前端 API](../../../api/frontend/)
- 想扩展平台前端：看 [扩展前端](../extend-frontend/)
